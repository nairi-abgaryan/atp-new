{% extends 'base.html.twig' %}
{% block title %}<title>Armenia Tree Project - Videos</title>{% endblock %}

{% block body %}

    {% include('interactive/secondary-menu.html.twig') %}

    <section class="bb-videos-wrapper">
        <div class="container">
            <h4>VIDEOS</h4>
            <div class="row">
                <div class="col-md-8 col-xs-12">
                    <div class="bb-video-one" style="background: linear-gradient( rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.2) ), url('../uploads/images/{{ top.Image }}') no-repeat; background-size: cover;">
                        <div class="title-and-play-button-wrapper">
                            {% autoescape %}
                                    <h3>{{ top.Title|raw }}</h3>
                            {% endautoescape %}
                            <a  data-toggle="modal" data-target="#bb-videos-one"><img class="play-button" src="../img/play_btn.svg" alt=""></a>
                        </div>
                    </div>
                    <div class="modal fade" id="bb-videos-one" tabindex="-1" role="dialog" aria-labelledby="bb-videos-one"
                         aria-hidden="true">
                        <div class="modal-dialogg">
                            <div class="modal-contentt">
                                <div class="modal-body">
                                    <button style="color: #ffffff; opacity: 1;" type="button" class="close" data-dismiss="modal"
                                            aria-hidden="true">&times;</button>
                                    <div class="video-container">
                                        <iframe width="560" height="315" src="{{ top.link }}" frameborder="0"  allowfullscreen></iframe>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 col-xs-12">
                    {% autoescape %}
                            <p>{{ top.Text|raw }}</p>
                    {% endautoescape %}
                </div>
            </div>
            <div class="row">
                {% for item in content %}
                    <div class="col-sm-4 col-xs-12">
                        <div class="bb-video-two" style="background: linear-gradient( rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.2) ), url('../uploads/images/{{ item.Image }}') no-repeat; background-size: cover;">
                            <div class="title-and-play-button-wrapper">
                                {% autoescape %}
                                        <h3>{{ item.Title|raw }}</h3>
                                {% endautoescape %}
                                <a  data-toggle="modal" data-target="#m{{ item.id }}"><img class="play-button" src="../img/play_btn.svg" alt=""></a>
                            </div>
                        </div>
                        <div class="modal fade" id="m{{ item.id }}" tabindex="-1" role="dialog" aria-labelledby="m{{ item.id }}"
                             aria-hidden="true">
                            <div class="modal-dialogg">
                                <div class="modal-contentt">
                                    <div class="modal-body">
                                        <button style="color: #ffffff; opacity: 1;" type="button" class="close" data-dismiss="modal"
                                                aria-hidden="true">&times;</button>
                                        <div class="video-container">
                                            <iframe width="560" height="315" src="{{ item.link }}" frameborder="0"  allowfullscreen></iframe>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>


    <!--Footer Section-->
    {% include('interactive/footer.html.twig') %}

    <!-- jQuery -->
    <script src="{{ asset('js/jquery.js') }}"></script>

    <!-- Lightbox sliider-->
    <script src="{{ asset('js/lightbox.js') }}"></script>

    <!-- Bootstrap Core JavaScript -->
    <script src="{{ asset('js/bootstrap.min.js') }}"></script>

    <!-- Custom Js Files-->
    <script src="{{ asset('js/main.js') }}"></script>

{% endblock %}